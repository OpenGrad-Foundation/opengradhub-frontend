"use client";

import React from "react";
import EmptyState from "@/components/dashboard/primitives/EmptyState";

export default function GovernmentTasks() {
  return <EmptyState helperText="Read-only role — no tasks" />;
}
